import PageTitle from "@/app/_components/page_title";

export default function ServicesLayout({ children }) {
    return (
        <>
            <PageTitle />
            {children}
        </>
    )
}
